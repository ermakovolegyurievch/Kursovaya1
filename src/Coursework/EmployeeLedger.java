package Coursework;

public class EmployeeLedger {
    public final Employee[] employees;

    private int size;

    public EmployeeLedger() {
        this.employees = new Employee[10];

        public void addEmployee(String surName, String firstName, String secondName, int department, int salary) {
            if (size >= contacts.length) {
                System.out.println("Нельзя добавить контакт, закончилось место");
            }
            Contact newContact = new Contact(contactNick, phone);
            contacts[size++] = newContact;
        }

        }
    }

